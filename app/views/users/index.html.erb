<h1>Users</h1>

<ul>
	<% @users.each do |u| %>
		<li>
		<%= u.name %> <button><%= link_to "View", user_path(u) %></button>
		</li>

	<%end%>

</ul>
<%= link_to "Sign Up", new_user_path %>
<%= link_to "plnLA Home", activities_path %>
